apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vllm-tls
  namespace: default
spec:
  secretName: vllm-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  commonName: llm.local
  dnsNames:
  - llm.local
